% function G = CreateDiscoveredGraph(env)

function G = CreateDiscoveredGraph(env)

    [envHeight, envLength] = size(env.map);
    
    for i = 1:envLength
        for j = 1:envHeight
            
            
            
        end
    end
    
    


end